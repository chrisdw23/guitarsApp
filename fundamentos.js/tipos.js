    const pi = 3.14

    let name = "Cristhian" // string
    let lastNmae = 'Mendoza' // string

    let isLate = true // boolean

    let age // undefined
    age = 20 // number

    // Arrays
    let numbers = [2, 5, 7, 9] // object - Array
    console.log(numbers.length)

    numbers.push(15) // inserta elemntos al final
    numbers.pop() // extre elemnto final
    numbers.shift() // extrae del inicio
    numbers.unshift(19) // inserta al incio

    numbers.push('otro')
    console.log(numbers)

    // mostrar string al reves 
    let nombre = 'crsithian'
    let nombreArray = nombre.split('')
    console.log(nombreArray)
    let array_reverse = nombreArray.reverse()
    console.log(array_reverse)
    let nombre_reverse = array reverse.join('')
    console.log(nombre_reverse)

    // Objetos
    const group = {
        grade: 4,
        name: 'A',
        career: 'TSU DMS',
        students: ['Alan', 'Denisse', 'Emmanuel'],
        profesor: 'Francisco'
    }
 
    console.log(group)
    console.log(group.profesor)
    console.log(group.students)
    console.log(group.career)

    group.tutor = 'Diana'
    group.pet = 'Cat'

    console.log(group)

